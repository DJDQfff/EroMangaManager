<Page x:ConnectionId='1'
    x:Class="EroMangaManager.WinUI3.Views.MainPageChildPages.Bookcase"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="using:EroMangaManager.WinUI3"
    xmlns:complexmanga="using:EroMangaManager.WinUI3.UserControls.MangaComplexDisplay"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:EroMangaManager.Core.Models"
    xmlns:uid="using:EroMangaManager.WinUI3.Strings"
    xmlns:usercontrols="using:EroMangaManager.WinUI3.UserControls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    NavigationCacheMode="Required"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="BookcaseTemplate1"                          >
            <complexmanga:MangaTagsView x:ConnectionId='5'                  />
        </DataTemplate>
        <DataTemplate x:Key="BookcaseTemplate0"                          >
            <complexmanga:BriefInfo x:ConnectionId='3'                  />
        </DataTemplate>
    </Page.Resources>
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock x:ConnectionId='6'
            x:Name="Bookcase_HintTextBlock"
            Grid.Row="0"
            Grid.RowSpan="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Style="{StaticResource HintTextBlockStyle}"
            Text="{uid:Strings Uid=NoSelectedFolder}"
            Visibility="Visible" />
        <CommandBar x:ConnectionId='7'
            x:Name="BookcaseCommandBar"
            Grid.Row="0"
            VerticalAlignment="Stretch"
            VerticalContentAlignment="Center">
            <CommandBar.Content>
                <ComboBox x:ConnectionId='9'
                    x:Name="combochangefolder"
                    DisplayMemberPath="FolderPath"
                                                                                                    
                                                                                                                                                                    
                                                                          />
                <!--<TextBlock x:Name="CommandText"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           HorizontalTextAlignment="Center"
                           Text="{x:Bind MangasGroup.ShowString, Mode=OneWay}"
                           TextAlignment="Center" />-->
            </CommandBar.Content>
            <AppBarElementContainer VerticalAlignment="Center" VerticalContentAlignment="Center">
                <usercontrols:MangaOpenButton  ToolTipService.ToolTip="{uid:Strings Uid=DefaultWay}"/>
            </AppBarElementContainer>
            <AppBarButton
                AllowFocusOnInteraction="True"
                Content="Filter"
                Icon="Filter">
                <AppBarButton.Flyout>
                    <Flyout>
                        <StackPanel Width="200" Orientation="Vertical">
                            <TextBox PlaceholderText="含有内容" />

                            <RadioButtons MaxColumns="2" />
                            <!--<Grid>
                                <Grid.Resources>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Height" Value="40" />
                                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                            -->
                            <!--<Setter Property="Content">
                                            <Setter.Value>
                                                <Rectangle Fill="{ThemeResource SystemAccentColor}" />
                                            </Setter.Value>
                                        </Setter>-->
                            <!--
                                    </Style>
                                </Grid.Resources>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />

                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <ToggleButton IsEnabledChanged="ToggleButton_IsEnabledChanged"
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Content="size降" />
                                <ToggleButton IsEnabledChanged="ToggleButton_IsEnabledChanged"
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    Content="size升" />
                                <ToggleButton IsEnabledChanged="ToggleButton_IsEnabledChanged"
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Content="chapter降" />
                                <ToggleButton
                                    Grid.Row="1" IsEnabledChanged="ToggleButton_IsEnabledChanged"
                                    Grid.Column="1"
                                    Content="chapter升" />

                                <ToggleButton IsEnabledChanged="ToggleButton_IsEnabledChanged"
                                    Grid.Row="2"
                                    Grid.Column="0"
                                    Content="images降" />
                                <ToggleButton IsEnabledChanged="ToggleButton_IsEnabledChanged"
                                    Grid.Row="2"
                                    Grid.Column="1"
                                    Content="images升" />
                            </Grid>-->
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarButton
                AllowFocusOnInteraction="True"
                Icon="Switch"
                Label="{uid:Strings Uid=SwitchViewMode}"
                Visibility="Collapsed">
                <AppBarButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:ConnectionId='12'
                                                        
                            Tag="0"
                            Text="{uid:Strings Uid=ThumanailViewMode}" />
                        <MenuFlyoutItem x:ConnectionId='13'
                                                        
                            Tag="1"
                            Text="{uid:Strings Uid=TagsViewMode}" />
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarButton x:ConnectionId='10'
                             
                Icon="Sort"
                Label="{uid:Strings Uid=ChangeSortMethod}" />
            <AppBarButton x:ConnectionId='11'
                x:Name="TranslateButton"
                x:Load="False"
                                              
                Icon="Font"
                Label="{uid:Strings Uid=Translate}" />
        </CommandBar>
        <GridView x:ConnectionId='8'
            x:Name="Bookcase_GridView"
            Grid.Row="1"
            Margin="4"
            x:FieldModifier="public"
            IsItemClickEnabled="True"
            ItemContainerStyle="{ThemeResource GridViewItemRevealStyle}"
            SelectionMode="Single" />
    </Grid>
</Page>

