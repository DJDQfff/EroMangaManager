<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:ConnectionId='1'
    x:Class="EroMangaManager.WinUI3.UserControls.MangaComplexDisplay.MangaTagsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:EroMangaManager.WinUI3.UserControls"
    xmlns:mangainfo="using:EroMangaManager.WinUI3.UserControls.MangaBasicInfo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:strings="using:EroMangaManager.WinUI3.Strings"
    mc:Ignorable="d">
    <UserControl.Resources />
    <!--  这个控件是专门用来给overviewinformation调用的，所以不能用coverwithcontextflyout控件  -->
    <Grid Width="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <mangainfo:MangaName x:ConnectionId='2' FontSize="20"                                     />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <mangainfo:CoverWithContextFlyout x:ConnectionId='4' Grid.Column="0"                                      />
            <ListView x:ConnectionId='5' Grid.Column="1"                                               >
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate                      >
                        <local:TagTextBlock x:ConnectionId='7'                 />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <!--<local:TagItemsRepeater Manga="{x:Bind Manga,Mode=OneWay}" />-->
        </Grid>
        <mangainfo:SizeImagesChaptersAmount x:ConnectionId='3' Grid.Row="2"                                           />
    </Grid>
</UserControl>

