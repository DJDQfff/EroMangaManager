<Page x:ConnectionId='1'
    x:Class="EroMangaManager.WinUI3.Views.MainPageChildPages.TagsManagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entity="using:EroMangaDatabase.Entities"
    xmlns:uid="using:EroMangaManager.WinUI3.Strings"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    NavigationCacheMode="Required"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="entity"                                >
            <Grid x:ConnectionId='3'>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:ConnectionId='4'                                           />
                <Button Grid.Column="1">
                    <Button.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem x:ConnectionId='5'
                                                              
                                                      
                                Icon="Remove"
                                Text="{uid:Strings Uid=Remove}" />
                            <MenuFlyoutItem x:ConnectionId='6'
                                                              
                                                      
                                Icon="Rename"
                                Text="{uid:Strings Uid=Rename}" />
                        </MenuFlyout>
                    </Button.Flyout>
                    <SymbolIcon Symbol="More" />
                </Button>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0" />
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="{uid:Strings Uid=Import}">
                    <Button.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem x:ConnectionId='13'
                                                              
                                Tag="作者"
                                Text="{uid:Strings Uid=Author}" />
                            <MenuFlyoutItem x:ConnectionId='14'
                                                              
                                Tag="汉化组"
                                Text="{uid:Strings Uid=ChineseTranslateTeam}" />
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
                <TextBox x:ConnectionId='10'
                    x:Name="NewCategoryTextBox"
                    Grid.Column="1"
                    HorizontalAlignment="Stretch" />
                <Button x:ConnectionId='11'
                    Grid.Column="2"
                                        
                    Content="{uid:Strings Uid=AddCategory}" />
                <Button x:ConnectionId='12'
                    Grid.Column="3"
                                                                      
                    Content="{uid:Strings Uid=Save}" />
            </Grid>
            <ListView x:ConnectionId='8'
                x:Name="Category_ListVIew"
                Grid.Row="1"
                IsItemClickEnabled="True"
                                                       
                ItemTemplate="{StaticResource entity}"
                                                                          >
                <ListView.Header>
                    <ListViewItem x:ConnectionId='9' Content="{uid:Strings Uid=UnCategoryed}"                              />
                </ListView.Header>
            </ListView>
        </Grid>
        <ListView x:ConnectionId='7'
            x:Name="Tag_ListView"
            Grid.Column="1"
                                               
            ItemContainerStyle="{StaticResource ListViewItemExpanded}"
            SelectionMode="Multiple" />
    </Grid>
    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ComboBox Header="分类" />

        <ScrollViewer
            x:Name="WrapScrollParent"
            Grid.Column="1"
            Visibility="Collapsed">
            <ItemsRepeater x:Name="WrapRepeater" ItemsSource="{x:Bind ColorsCollection, Mode=OneWay}">
                <ItemsRepeater.Layout>
                    <controls:WrapLayout
                        x:Name="Wrap"
                        HorizontalSpacing="6"
                        VerticalSpacing="6" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Border Background="{x:Bind Converter={StaticResource randomcolor}}">
                            <TextBlock
                                Margin="6,4,4,4"
                                FontSize="16"
                                Text="{x:Bind}">
                                <TextBlock.ContextFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem
                                            Click="MenuFlyoutItem_Click"
                                            DataContext="{x:Bind}"
                                            Text="查找此标签" />
                                    </MenuFlyout>
                                </TextBlock.ContextFlyout>
                            </TextBlock>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>-->
</Page>

