<Page x:Class="EroMangaManager.Views.SettingPageChildPages.ErrorZipPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:root="using:EroMangaManager"
      xmlns:manga="using:EroMangaManager.Models"
      mc:Ignorable="d"
      Background="Transparent">

    <Grid>
        <ListView x:Name="ErrorZipVIewListView"
                  ItemsSource="{x:Bind root:MainPage.current.collectionObserver.NonZipList }">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="manga:MangaBook">
                    <TextBlock Text="{x:Bind MangaName}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>