<Page x:Class="EroMangaManager.UWP.Views.MainPageChildPages.SettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:uid="using:EroMangaManager.UWP.LocalizationWords"
      Margin="36,0,0,0"
      mc:Ignorable="d">
    <Grid>
        <NavigationView  PaneDisplayMode="Top"
                         ItemInvoked="NavigationView_ItemInvoked"
                         IsSettingsVisible="False"
                         IsBackButtonVisible="Collapsed">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="CommonSettingNavigationViewItem"
                                    Content="{uid:Resources Resources=CommonSetting}" />
                <NavigationViewItem x:Name="SettingFilterImageButton"
                                    Content="{uid:Resources Resources=FilteredImages}" />
                <NavigationViewItem x:Name="SettingTagButton" Visibility="Collapsed"
                                    Content="{uid:Resources Resources=Tags}"/>
                <NavigationViewItem x:Name="ErrorZipPageButton"
                                    Content="{uid:Resources Resources=ErrorZips}" />
            </NavigationView.MenuItems>
            <NavigationView.Content>
                <Frame x:Name="SettingFrame"
                       Loaded="SettingFrame_Loaded"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       HorizontalContentAlignment="Stretch"
                       VerticalContentAlignment="Stretch" />
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Page>